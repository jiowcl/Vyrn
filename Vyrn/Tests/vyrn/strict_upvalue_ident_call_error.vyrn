-- vyrn: 12 strict
struct Vec2
    x: number
    y: number
end

def accept(v: Vec2)
    print(v.x)
end

def outer()
    let cb = accept
    def inner()
        let n: number = 1
        cb(n)
    end
    inner()
end

outer()
